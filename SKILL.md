---
name: ai-lesson-planner
description: "企业级 AI 备课与教学内容生产：把课程标准、教材、知识库或已有教案归一为 LessonBrief，经目标反向设计、逐节生产、学科交互插件、质量门禁、人工审核和版本审计，交付可追溯的课程包。适用于教师备课、教研内容生产、课程改版和批量课程建设。"
argument-hint: "[课程主题/教材/已有教案] [目标学员] [课时与交付形式]"
---

# 企业级 AI 备课与教学内容生产

## 1. 定位

把备课从“一次对话生成一篇教案”，升级为**有依据、有结构、有检查、有审核、有版本**的教学内容生产流程。

核心原则：

1. **先定义学习结果，再生产内容**：目标、活动、练习和评价必须对齐。
2. **事实来自来源，表达可以由 AI 组织**：不得把模型记忆当课程依据。
3. **计算与可视化交给确定性工具**：数学、物理、化学结果尽量同源计算并自检。
4. **AI 产初稿，人做教学决策**：未经授权，不把 AI 草稿标成正式发布版。
5. **每一步留下结构化产物**：支持复核、局部重跑、版本比较和批量复用。

## 2. 适用范围与边界

适用：

- 单节课、系列课、录播课、直播课、图文课的备课与改版；
- 教材/课程标准驱动的教案、讲稿、活动、练习和互动素材规划；
- 教师、教研员、课程运营之间的内容生产与审核；
- 低风险教学辅助内容的批量初稿生产。

不直接承诺：

- 在没有 API、账号或目标平台适配器时“自动发布到 LMS”；
- 在没有来源时保证事实正确；
- 自动替代教师或教研员完成高风险内容审核；
- 仅凭一次生成就得到可上线课程；
- 编造学校、教师、学生、课程效果或上线数据。

## 3. 三种入口与输出模式

### 3.1 输入入口

1. **主题入口**：只有课程主题。先补齐学员、目标、课时、来源和约束。
2. **来源入口（优先）**：教材、课程标准、知识库、PPT、讲稿或链接。建立来源清单后再生成。
3. **改版入口**：已有教案/课程包。先诊断目标对齐、事实、节奏和评价问题，再局部重写。

### 3.2 输出模式

- `quick-draft`：课程结构 + 单节教案草稿；用于探索，不可标记为正式版。
- `standard`（默认）：完整课程包 + 来源 + 质量报告 + 审核清单。
- `enterprise`：在 standard 基础上增加角色、版本、审批、批量模板、成本与审计字段。

用户未指定时使用 `standard`。批量生成前必须先完成并确认一个样板课。

## 4. 工作流

### 阶段 0：生成并确认 LessonBrief

先整理以下信息；缺失的关键项最多集中追问 5 个问题，不要边生成边猜：

```yaml
lesson_brief:
  title: 课程主题
  audience: 年龄/年级/先备知识/典型困难/无障碍需要
  business_context: 学校/培训/企业学习等，可空但不得编造
  delivery_mode: 录播/直播/面授/图文/混合
  total_duration_minutes: 45
  lesson_count: 1
  learning_result: 学完后能完成的真实任务
  source_policy: source-grounded | provided-only | exploratory
  required_outputs: [outline, teacher_guide, script, activities, practice]
  constraints: 课程标准、禁用内容、语气、设备与平台限制
  review_roles: [teacher, subject_reviewer]
```

必须把 LessonBrief 回显给用户确认，尤其是目标学员、学习结果、来源策略和交付范围。

### 阶段 1：建立 Source Pack

给每个来源分配 `S1`、`S2` 等 ID，并标注：

- 名称、作者/机构、版本或日期；
- 来源类型：课程标准/教材/官方文档/内部知识库/教师经验；
- 权威性与适用范围；
- 是否允许进入公开交付物；
- 关键结论和可引用位置。

规则：

- 关键事实、定义、公式和时效性结论必须能追溯到来源；
- 来源冲突时列出冲突，不擅自拼成“统一答案”；
- 内部资料和学生数据默认不进入外部模型或公开产物；
- 来源不足时明确标记 `SOURCE_GAP`，不得用流畅表达掩盖证据缺口。

### 阶段 2：反向设计学习目标

每个目标必须包含：

```yaml
objective:
  id: O1
  statement: 学员能在给定场景中……
  bloom_level: remember | understand | apply | analyze | evaluate | create
  success_criteria: 可观察、可判定的达成标准
  evidence: 用什么任务或题目证明达成
  prerequisite_ids: []
  source_ids: [S1]
```

禁止使用无法验证的“了解、熟悉、掌握”作为唯一目标。若必须使用，改写为能观察的行为。

### 阶段 3：设计课程结构与节奏

按“目标 → 证据 → 活动 → 内容”设计，而不是先堆知识点。每节课至少包括：

1. 激活先备知识或诊断问题；
2. 明确本节可执行目标；
3. 核心讲解与示例；
4. 引导练习；
5. 独立练习或迁移任务；
6. 形成性检测与反馈；
7. 总结和后续行动。

控制认知负荷：一段只承担一个教学任务；复杂概念分块；新概念、例题、练习之间留出处理时间。

### 阶段 4：逐节生产，不一次生成整门课

每节课生成以下可组合产物：

- `teacher_guide`：目标、难点、时间分配、提问、误区和应对；
- `student_handout`：关键概念、示例、活动和课后任务；
- `script`：钩子—解释—示例—练习—总结，标注预计时长；
- `activities`：活动步骤、材料、分组方式、教师观察点；
- `practice`：与目标绑定的形成性练习，不代替正式测评系统；
- `interactive_artifact_plan`：需要图表、仿真、3D 或动态演示时生成。

每个活动和练习都必须引用 `objective_ids`；每个事实性讲解应引用 `source_ids`。

### 阶段 5：调用学科交互插件（可选）

当安装了兼容插件且用户同意使用时，可把专门学科任务委派给确定性工具：

- 立体几何 → `edu-solid-geometry`：SymPy 精确计算 + Three.js 3D 分步演示；
- 解析几何 → `edu-analytic-geometry`：SymPy + 2D 动态画板 + 参数滑块；
- 化学反应 → `edu-chem-reaction`：自动配平、原子守恒校验 + 3D 微观反应；
- 物理演示 → 生成结构化仿真规格；只有实际存在适配器时才调用。

委派前输出结构化 `artifact_spec`，委派后检查“计算结果、讲解步骤、最终答案、交互显示”是否一致。插件不存在时，输出静态分镜/交互规格作为降级方案，**不得假装已生成互动网页**。

详细协议见 `references/interactive-artifacts.md`。

### 阶段 6：质量门禁

按 `references/quality-gates.md` 评分并执行硬门禁。

硬门禁包括：

- 关键事实无来源或与来源冲突；
- 学习目标没有证据或完全没有活动覆盖；
- 计算答案与讲解/可视化不一致；
- 面向未成年人却包含不必要的个人数据；
- 高风险内容未经专业人员审核；
- 声称已发布、已测试或已取得效果，但没有证据。

总分建议达到 85/100 且无硬失败，才进入人工审核。未通过时只重跑失败模块，不整包重写。

### 阶段 7：人工审核与版本流转

推荐状态：

```text
draft_ai → teacher_review → subject_review → approved → published → archived
```

- AI 只能生成 `draft_ai`；
- 教师确认学情、案例、节奏和可教性；
- 教研员确认学科准确性和课程标准；
- 每次修改记录版本、修改人、原因和时间；
- 发布后根据课堂数据和测评结果创建新版本，不覆盖历史版本。

### 阶段 8：交付课程包

标准交付目录：

```text
lesson-package/
├── lesson-brief.md
├── source-pack.md
├── curriculum-map.md
├── teacher-guide.md
├── student-handout.md
├── script.md
├── activities.md
├── formative-practice.json
├── interactive-artifacts/
├── quality-report.md
├── review-log.md
└── lesson-package.json
```

若当前环境不能创建文件，按相同标题输出 Markdown，并附结构化 JSON。

## 5. 企业级治理

### 角色建议

| 角色 | 权限 |
|---|---|
| Teacher | 创建、编辑、提交审核 |
| Subject Reviewer | 学科审核、退回、批准 |
| Curriculum Admin | 管理模板、课程标准和发布规则 |
| Organization Admin | 模型、数据、成本和权限策略 |
| Viewer/Learner | 只访问已发布内容 |

### 模型与成本策略

- 结构抽取、格式转换优先使用低成本模型；
- 学科推理、关键审校使用能力更强的模型或确定性工具；
- 记录模型、版本、Prompt/Skill 版本、调用时间和估算成本；
- 后端失败不得静默换模型，需记录降级原因；
- 本地隐私场景可以使用本地模型，但质量门禁不降低。

### 数据安全

- 不把学生姓名、联系方式、成绩明细等直接放入公开模型；
- 使用最小必要数据和匿名 ID；
- 区分公开教材、授权内容、内部资料和个人数据；
- 未确认版权/授权的材料不得进入公开课程包。

## 6. 结构化输出与验证

`lesson-package.json` 字段见 `references/lesson-package-schema.md`。生成后运行：

```bash
python3 scripts/validate_lesson_package.py path/to/lesson-package.json
```

验证器检查 ID 引用、目标覆盖、评价覆盖、课时和审核状态。通过验证不代表内容事实正确，仍需来源核验和人工审核。

## 7. 面试/复盘指标

只报告真实采集的数据，不编造：

- 单节课从创建到提交审核的耗时；
- AI 初稿采用率和教师修改率；
- 一次审核通过率与主要退回原因；
- 目标—活动—评价覆盖率；
- 事实错误率和来源缺口数；
- 单节课模型成本；
- 模板复用率、发布周期和教师活跃度。

## 8. 失败模式

- 直接从主题生成长教案，没有 Brief 和来源；
- 目标写得漂亮，但活动和评价无法证明目标达成；
- 用 AI 编造“真实案例”或学生数据；
- 数学/化学答案由模型心算，讲解与图形不一致；
- 批量生成几十节课后才第一次审核；
- 把“生成成功”当作“教学有效”；
- 没有权限、版本、审核和成本记录，却称为企业级流程。

## 9. 相关文件

- `references/lesson-package-schema.md`：结构化课程包字段；
- `references/quality-gates.md`：评分量表与硬门禁；
- `references/interactive-artifacts.md`：学科插件委派协议；
- `scripts/validate_lesson_package.py`：课程包结构校验；
- `examples/lesson-package.example.json`：最小完整示例。
